version: 0.0
os: linux
files:
  - source: /
    destination: /
hooks:
  ApplicationStop:
    - location: applicationstop.sh
      timeout: 300
			runas: root
  BeforeInstall:
    - location: beforeinstall.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: applicationstart.sh
      timeout: 300
      runas: root
